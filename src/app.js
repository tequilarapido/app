(function (window) {
  'use strict';

  /**
   * Object exported into Window as App
   *
   * @type {object}
   */
  var app = {};

  /**
   * Init and inject into global (window) scope
   */
  window.App = app;

}(window));
